<!DOCTYPE html>
<html>
<head>
  <!-- Required meta tags for Bootstrap-->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <title>My Properties</title>
</head>
<body class="container pt-5">
  <h1>My Properties</h1>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Address</th>
        <th scope="col">Neighborhood</th>
        <th scope="col">Sqft</th>
        <th scope="col">Parking</th>
        <th scope="col">Public Transportation</th>
        <th scope="col">Workspaces</th>
        <th scope="col">Actions</th>
        <th scope="col">Image</th> <!-- You may add this for header of Image column -->
      </tr>
    </thead>
    <tbody>
      <% for (let id in properties) { %>
        <tr>
          <td>
            <%= properties[id].address1 %>
            <% if (properties[id].address2) { %>
              <%= properties[id].address2 %>,
            <% } %>
            <%= properties[id].city %>,
            <%= properties[id].state %>,
            <%= properties[id].postalcode %>
          </td>
          <td><%= properties[id].neighborhood %></td>
          <td><%= properties[id].sqft %></td>
          <td><%= properties[id].parking ? 'Yes' : 'No' %></td>
          <td><%= properties[id].publicTrans ? 'Yes' : 'No' %></td>
          <td><%= Object.keys(properties[id].workspaces).length %></td>
          <td>
            <a class="btn btn-primary" href="/properties/<%= id %>/workspaces">View Workspaces</a>
            <a class="btn btn-warning" href="/properties/<%= id %>/edit">Edit</a>
            <form action="/properties/<%= id %>/delete" method="POST" style="display: inline;">
              <input class="btn btn-danger" type="submit" value="Delete">
            </form>
          </td>
          <td><img src="<%= properties[id].imageUrl %>" alt="Image of <%= properties[id].address1 %>" style="width: 100px; height: 100px;"></td> <!-- Add this line -->
        </tr>
      <% } %>
    </tbody>
  </table>
  <a class="btn btn-success" href="/properties/new">Add Property</a>
  <a class="btn btn-secondary" href="/owner/workspaces">List all Workspaces</a>
</body>
</html>
